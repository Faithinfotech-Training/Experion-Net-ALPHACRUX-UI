import { MedPrescriptionList } from './med-prescription-list';

describe('MedPrescriptionList', () => {
  it('should create an instance', () => {
    expect(new MedPrescriptionList()).toBeTruthy();
  });
});
